LLM APIs are fundamentally stateless, meaning they forget past inputs instantly. LangChain Memory components solve this by automatically storing and appending past conversation history to new requests. Agents take this a step further by using the LLM as a reasoning engine to autonomously decide *which* external tools (like databases, calculators, or web browsers) to invoke to complete a user's task.