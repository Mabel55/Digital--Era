Training a machine learning model requires splitting your data into a training set and a testing set (typically an 80/20 split). To verify if a model is genuinely learning or just memorizing data (overfitting), developers use evaluation metrics. For classification tasks, these metrics include Accuracy, Precision (minimizing false positives), Recall (minimizing false negatives), and the F1-Score, which balances both.