"""
Batch 3: C Programming, DSA, Data Science, AI Engineering
"""
import json, os

LESSONS_BATCH3 = {
    # ─── C PROGRAMMING ───
    "C Syntax": [
        {"title": "Hello World in C", "theory": "## Your First C Program\n\n```c\n#include <stdio.h>\n\nint main() {\n    printf(\"Hello, World!\\n\");\n    return 0;\n}\n```\n\n- `#include <stdio.h>` imports the standard I/O library\n- `int main()` is the entry point\n- `printf()` prints to the console\n- `return 0` means the program ran successfully", "instructions": "## Task: Greeting Program\n1. Write a C program that prints your name\n2. Print your age on a second line\n3. Use `\\n` for newlines", "starterCode": "#include <stdio.h>\n\nint main() {\n    printf(\"Name: ___\\n\");\n    printf(\"Age: ___\\n\");\n    return ___;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    printf(\"Name: Mabel\\n\");\n    printf(\"Age: 20\\n\");\n    return 0;\n}", "hint": "printf() prints text. \\n makes a new line. return 0 for success.", "rubric": "Program compiles. Prints name and age on separate lines."},
        {"title": "Variables & Types", "theory": "## C Data Types\n\n```c\nint age = 25;           // Integer\nfloat price = 19.99;    // Decimal (single precision)\ndouble pi = 3.14159;    // Decimal (double precision)\nchar grade = 'A';       // Single character\nchar name[] = \"Alice\";  // String (char array)\n```\n\nC is **statically typed** — you must declare the type before using a variable.", "instructions": "## Task: Variable Declaration\n1. Declare an `int` for year, `float` for temperature, `char` for initial\n2. Print each using the correct format specifier\n3. `%d` for int, `%f` for float, `%c` for char", "starterCode": "#include <stdio.h>\n\nint main() {\n    ___ year = 2024;\n    ___ temperature = 36.5;\n    ___ initial = 'M';\n    \n    printf(\"Year: %___\\n\", year);\n    printf(\"Temp: %___\\n\", temperature);\n    printf(\"Initial: %___\\n\", initial);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int year = 2024;\n    float temperature = 36.5;\n    char initial = 'M';\n    \n    printf(\"Year: %d\\n\", year);\n    printf(\"Temp: %.1f\\n\", temperature);\n    printf(\"Initial: %c\\n\", initial);\n    return 0;\n}", "hint": "int for integers, float for decimals, char for characters. %d, %f, %c.", "rubric": "Correct types declared. Format specifiers match types."},
        {"title": "Arithmetic Operations", "theory": "## Math in C\n\n```c\nint a = 10, b = 3;\nprintf(\"%d\\n\", a + b);   // 13\nprintf(\"%d\\n\", a - b);   // 7\nprintf(\"%d\\n\", a * b);   // 30\nprintf(\"%d\\n\", a / b);   // 3 (integer division!)\nprintf(\"%d\\n\", a % b);   // 1 (modulus)\n```\n\n**Warning:** Integer division truncates! `10 / 3 = 3`, not 3.33.", "instructions": "## Task: Calculator\n1. Declare two integers `a = 17` and `b = 5`\n2. Print sum, difference, product, quotient, and remainder\n3. Cast to float to show true division", "starterCode": "#include <stdio.h>\n\nint main() {\n    int a = 17, b = 5;\n    \n    printf(\"Sum: %d\\n\", a ___ b);\n    printf(\"Diff: %d\\n\", a ___ b);\n    printf(\"Product: %d\\n\", a ___ b);\n    printf(\"Quotient: %d\\n\", a ___ b);\n    printf(\"Remainder: %d\\n\", a ___ b);\n    printf(\"True div: %.2f\\n\", (float)a / b);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int a = 17, b = 5;\n    \n    printf(\"Sum: %d\\n\", a + b);\n    printf(\"Diff: %d\\n\", a - b);\n    printf(\"Product: %d\\n\", a * b);\n    printf(\"Quotient: %d\\n\", a / b);\n    printf(\"Remainder: %d\\n\", a % b);\n    printf(\"True div: %.2f\\n\", (float)a / b);\n    return 0;\n}", "hint": "+, -, *, / for math. % for remainder. (float) casts for true division.", "rubric": "All operations correct. True division shows 3.40."},
        {"title": "If/Else in C", "theory": "## Conditional Statements\n\n```c\nint score = 85;\n\nif (score >= 90) {\n    printf(\"Grade: A\\n\");\n} else if (score >= 80) {\n    printf(\"Grade: B\\n\");\n} else if (score >= 70) {\n    printf(\"Grade: C\\n\");\n} else {\n    printf(\"Grade: F\\n\");\n}\n```\n\nC uses `{}` braces for blocks (not indentation like Python).", "instructions": "## Task: Number Classifier\n1. Declare an integer `num = -5`\n2. Check if it's positive, negative, or zero\n3. If positive, check if it's even or odd", "starterCode": "#include <stdio.h>\n\nint main() {\n    int num = -5;\n    \n    if (num ___ 0) {\n        printf(\"Positive\\n\");\n        if (num ___ 2 == 0)\n            printf(\"Even\\n\");\n        else\n            printf(\"Odd\\n\");\n    } else if (num ___ 0) {\n        printf(\"Negative\\n\");\n    } else {\n        printf(\"Zero\\n\");\n    }\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int num = -5;\n    \n    if (num > 0) {\n        printf(\"Positive\\n\");\n        if (num % 2 == 0)\n            printf(\"Even\\n\");\n        else\n            printf(\"Odd\\n\");\n    } else if (num < 0) {\n        printf(\"Negative\\n\");\n    } else {\n        printf(\"Zero\\n\");\n    }\n    return 0;\n}", "hint": "> 0 for positive. < 0 for negative. % 2 == 0 for even.", "rubric": "Correctly identifies negative. Nested if for even/odd works."},
        {"title": "Loops in C", "theory": "## For and While Loops\n\n```c\n// For loop\nfor (int i = 0; i < 5; i++) {\n    printf(\"%d \", i);  // 0 1 2 3 4\n}\n\n// While loop\nint count = 10;\nwhile (count > 0) {\n    printf(\"%d \", count);\n    count--;\n}\n\n// Do-while (runs at least once)\ndo {\n    printf(\"Hello\\n\");\n} while (0);  // Runs once\n```", "instructions": "## Task: Multiplication Table\n1. Use a for loop to print the multiplication table for 7\n2. Format: `7 x 1 = 7`\n3. Loop from 1 to 10", "starterCode": "#include <stdio.h>\n\nint main() {\n    int num = 7;\n    \n    for (int i = ___; i ___ 10; i___) {\n        printf(\"%d x %d = %d\\n\", num, i, num ___ i);\n    }\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int num = 7;\n    \n    for (int i = 1; i <= 10; i++) {\n        printf(\"%d x %d = %d\\n\", num, i, num * i);\n    }\n    return 0;\n}", "hint": "Start at 1, go to 10 (<=). i++ increments. num * i for result.", "rubric": "Loop runs 10 times. Correct multiplication table printed."},
        {"title": "Functions in C", "theory": "## Defining Functions\n\n```c\n// Function declaration (prototype)\nint add(int a, int b);\n\n// Function definition\nint add(int a, int b) {\n    return a + b;\n}\n\n// Void function (no return)\nvoid greet(char name[]) {\n    printf(\"Hello, %s!\\n\", name);\n}\n\nint main() {\n    int sum = add(3, 5);\n    greet(\"Alice\");\n    return 0;\n}\n```", "instructions": "## Task: Math Functions\n1. Create `int max(int a, int b)` that returns the larger number\n2. Create `int factorial(int n)` using a loop\n3. Create `int is_prime(int n)` that returns 1 or 0\n4. Test all three", "starterCode": "#include <stdio.h>\n\nint max(int a, int b) {\n    return (a ___ b) ? a : b;\n}\n\nint factorial(int n) {\n    int result = 1;\n    for (int i = 2; i ___ n; i++) {\n        result ___ i;\n    }\n    return result;\n}\n\nint is_prime(int n) {\n    if (n < 2) return 0;\n    for (int i = 2; i * i ___ n; i++) {\n        if (n ___ i == 0) return 0;\n    }\n    return 1;\n}\n\nint main() {\n    printf(\"Max(7,12): %d\\n\", max(7, 12));\n    printf(\"5!: %d\\n\", factorial(5));\n    printf(\"Is 17 prime? %d\\n\", is_prime(17));\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint max(int a, int b) {\n    return (a > b) ? a : b;\n}\n\nint factorial(int n) {\n    int result = 1;\n    for (int i = 2; i <= n; i++) {\n        result *= i;\n    }\n    return result;\n}\n\nint is_prime(int n) {\n    if (n < 2) return 0;\n    for (int i = 2; i * i <= n; i++) {\n        if (n % i == 0) return 0;\n    }\n    return 1;\n}\n\nint main() {\n    printf(\"Max(7,12): %d\\n\", max(7, 12));\n    printf(\"5!: %d\\n\", factorial(5));\n    printf(\"Is 17 prime? %d\\n\", is_prime(17));\n    return 0;\n}", "hint": "> for comparison. *= to accumulate. i*i <= n for prime check. % for divisibility.", "rubric": "Max returns 12. Factorial returns 120. is_prime(17) returns 1."}
    ],
    "Pointers Basics": [
        {"title": "What are Pointers?", "theory": "## Pointers: Memory Addresses\nA pointer stores the memory address of another variable.\n\n```c\nint age = 25;\nint *ptr = &age;  // ptr holds the ADDRESS of age\n\nprintf(\"%d\\n\", age);    // 25 (the value)\nprintf(\"%p\\n\", ptr);    // 0x7fff... (the address)\nprintf(\"%d\\n\", *ptr);   // 25 (dereference: value AT the address)\n```\n\n- `&` = \"address of\"\n- `*` = \"value at address\" (dereference)", "instructions": "## Task: Pointer Basics\n1. Declare an int variable and a pointer to it\n2. Print the value, address, and dereferenced pointer\n3. Change the value through the pointer", "starterCode": "#include <stdio.h>\n\nint main() {\n    int num = 42;\n    int *ptr = ___num;  // Point to num\n    \n    printf(\"Value: %d\\n\", num);\n    printf(\"Address: %p\\n\", ptr);\n    printf(\"Via pointer: %d\\n\", ___ptr);\n    \n    // Change value through pointer\n    ___ptr = 100;\n    printf(\"New value: %d\\n\", num);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int num = 42;\n    int *ptr = &num;  // Point to num\n    \n    printf(\"Value: %d\\n\", num);\n    printf(\"Address: %p\\n\", ptr);\n    printf(\"Via pointer: %d\\n\", *ptr);\n    \n    // Change value through pointer\n    *ptr = 100;\n    printf(\"New value: %d\\n\", num);\n    return 0;\n}", "hint": "& gets address. * dereferences (gets/sets value at address).", "rubric": "Pointer assigned. Dereference reads 42. Assignment through pointer changes num to 100."},
        {"title": "Pointers & Functions", "theory": "## Pass by Reference\nC is pass-by-value by default. Use pointers to modify variables in functions.\n\n```c\nvoid swap(int *a, int *b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\nint main() {\n    int x = 10, y = 20;\n    swap(&x, &y);\n    printf(\"%d %d\\n\", x, y);  // 20 10\n}\n```", "instructions": "## Task: Swap & Increment\n1. Create a `swap(int *a, int *b)` function\n2. Create an `increment(int *value, int amount)` function\n3. Test both with variables", "starterCode": "#include <stdio.h>\n\nvoid swap(int *a, int *b) {\n    int temp = ___a;\n    ___a = ___b;\n    ___b = temp;\n}\n\nvoid increment(int *value, int amount) {\n    ___value += amount;\n}\n\nint main() {\n    int x = 10, y = 20;\n    printf(\"Before: x=%d, y=%d\\n\", x, y);\n    swap(___x, ___y);\n    printf(\"After swap: x=%d, y=%d\\n\", x, y);\n    increment(___x, 5);\n    printf(\"After increment: x=%d\\n\", x);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nvoid swap(int *a, int *b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\nvoid increment(int *value, int amount) {\n    *value += amount;\n}\n\nint main() {\n    int x = 10, y = 20;\n    printf(\"Before: x=%d, y=%d\\n\", x, y);\n    swap(&x, &y);\n    printf(\"After swap: x=%d, y=%d\\n\", x, y);\n    increment(&x, 5);\n    printf(\"After increment: x=%d\\n\", x);\n    return 0;\n}", "hint": "* to dereference inside function. & to pass address from main.", "rubric": "Swap exchanges values. Increment adds through pointer."},
        {"title": "Pointer Arithmetic", "theory": "## Moving Through Memory\nPointers can be incremented to move through arrays.\n\n```c\nint arr[] = {10, 20, 30, 40, 50};\nint *ptr = arr;  // Points to first element\n\nprintf(\"%d\\n\", *ptr);       // 10\nprintf(\"%d\\n\", *(ptr + 1)); // 20\nprintf(\"%d\\n\", *(ptr + 2)); // 30\n\nptr++;  // Move to next element\nprintf(\"%d\\n\", *ptr);       // 20\n```", "instructions": "## Task: Array Traversal with Pointers\n1. Create an array of 5 integers\n2. Use a pointer to iterate through the array\n3. Print each element and its address", "starterCode": "#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = ___;\n    \n    for (int i = 0; i < 5; i++) {\n        printf(\"arr[%d] = %d (addr: %p)\\n\", i, *(ptr + ___), (ptr + ___));\n    }\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr;\n    \n    for (int i = 0; i < 5; i++) {\n        printf(\"arr[%d] = %d (addr: %p)\\n\", i, *(ptr + i), (ptr + i));\n    }\n    return 0;\n}", "hint": "arr decays to pointer to first element. *(ptr + i) accesses element i.", "rubric": "All 5 elements printed with addresses. Pointer arithmetic correct."},
        {"title": "NULL Pointers", "theory": "## Safe Pointer Usage\n\n```c\nint *ptr = NULL;  // Points to nothing\n\n// Always check before dereferencing!\nif (ptr != NULL) {\n    printf(\"%d\\n\", *ptr);\n} else {\n    printf(\"Pointer is NULL!\\n\");\n}\n\n// After freeing memory\nfree(ptr);\nptr = NULL;  // Prevent dangling pointer\n```\n\n**Never dereference a NULL pointer!** It causes a segfault.", "instructions": "## Task: Safe Pointer Functions\n1. Create a function `safe_print(int *ptr)` that checks for NULL before printing\n2. Create a function `safe_divide(int a, int b, int *result)` that stores result via pointer\n3. Return 0 on error (division by zero), 1 on success", "starterCode": "#include <stdio.h>\n\nvoid safe_print(int *ptr) {\n    if (ptr ___ NULL) {\n        printf(\"Value: %d\\n\", ___ptr);\n    } else {\n        printf(\"NULL pointer!\\n\");\n    }\n}\n\nint safe_divide(int a, int b, int *result) {\n    if (b ___ 0) return 0;\n    ___result = a / b;\n    return 1;\n}\n\nint main() {\n    int x = 42;\n    safe_print(&x);\n    safe_print(NULL);\n    \n    int result;\n    if (safe_divide(10, 3, &result))\n        printf(\"10/3 = %d\\n\", result);\n    if (!safe_divide(10, 0, &result))\n        printf(\"Division by zero!\\n\");\n    return 0;\n}", "solution": "#include <stdio.h>\n\nvoid safe_print(int *ptr) {\n    if (ptr != NULL) {\n        printf(\"Value: %d\\n\", *ptr);\n    } else {\n        printf(\"NULL pointer!\\n\");\n    }\n}\n\nint safe_divide(int a, int b, int *result) {\n    if (b == 0) return 0;\n    *result = a / b;\n    return 1;\n}\n\nint main() {\n    int x = 42;\n    safe_print(&x);\n    safe_print(NULL);\n    \n    int result;\n    if (safe_divide(10, 3, &result))\n        printf(\"10/3 = %d\\n\", result);\n    if (!safe_divide(10, 0, &result))\n        printf(\"Division by zero!\\n\");\n    return 0;\n}", "hint": "!= NULL checks if pointer is valid. == 0 checks for division by zero.", "rubric": "NULL check prevents crash. Division by zero handled safely."},
        {"title": "Double Pointers", "theory": "## Pointer to a Pointer\n\n```c\nint val = 42;\nint *ptr = &val;    // Pointer to int\nint **dptr = &ptr;  // Pointer to pointer\n\nprintf(\"%d\\n\", **dptr);  // 42\n```\n\nDouble pointers are used when a function needs to modify a pointer itself.", "instructions": "## Task: Dynamic Allocation via Double Pointer\n1. Create a function that allocates an array via double pointer\n2. Fill it with values\n3. Print and free the array", "starterCode": "#include <stdio.h>\n#include <stdlib.h>\n\nvoid create_array(int **arr, int size) {\n    *arr = (int *)malloc(size * sizeof(int));\n    for (int i = 0; i < size; i++) {\n        (*arr)[i] = (i + 1) * 10;\n    }\n}\n\nint main() {\n    int *numbers = ___;\n    int size = 5;\n    \n    create_array(___numbers, size);\n    \n    for (int i = 0; i < size; i++) {\n        printf(\"%d \", numbers[i]);\n    }\n    printf(\"\\n\");\n    \n    free(numbers);\n    return 0;\n}", "solution": "#include <stdio.h>\n#include <stdlib.h>\n\nvoid create_array(int **arr, int size) {\n    *arr = (int *)malloc(size * sizeof(int));\n    for (int i = 0; i < size; i++) {\n        (*arr)[i] = (i + 1) * 10;\n    }\n}\n\nint main() {\n    int *numbers = NULL;\n    int size = 5;\n    \n    create_array(&numbers, size);\n    \n    for (int i = 0; i < size; i++) {\n        printf(\"%d \", numbers[i]);\n    }\n    printf(\"\\n\");\n    \n    free(numbers);\n    return 0;\n}", "hint": "Initialize to NULL. Pass &numbers (address of pointer). free() deallocates.", "rubric": "Array allocated via double pointer. Values 10 20 30 40 50 printed. Memory freed."}
    ],
    "Arrays & Strings": [
        {"title": "C Arrays", "theory": "## Arrays in C\nArrays store multiple values of the same type.\n\n```c\nint scores[5] = {90, 85, 78, 92, 88};\nfloat temps[3] = {36.5, 37.0, 36.8};\n\n// Access elements (0-indexed)\nprintf(\"%d\\n\", scores[0]);  // 90\nscores[2] = 80;              // Modify\n\n// Array size\nint size = sizeof(scores) / sizeof(scores[0]);  // 5\n```", "instructions": "## Task: Array Operations\n1. Create an array of 5 test scores\n2. Calculate and print the average\n3. Find and print the highest score", "starterCode": "#include <stdio.h>\n\nint main() {\n    int scores[] = {85, 92, 78, 95, 88};\n    int size = sizeof(scores) / sizeof(scores[___]);\n    int sum = 0, max = scores[0];\n    \n    for (int i = 0; i < ___; i++) {\n        sum ___ scores[i];\n        if (scores[i] ___ max)\n            max = scores[i];\n    }\n    \n    printf(\"Average: %.1f\\n\", (float)sum / size);\n    printf(\"Highest: %d\\n\", max);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int scores[] = {85, 92, 78, 95, 88};\n    int size = sizeof(scores) / sizeof(scores[0]);\n    int sum = 0, max = scores[0];\n    \n    for (int i = 0; i < size; i++) {\n        sum += scores[i];\n        if (scores[i] > max)\n            max = scores[i];\n    }\n    \n    printf(\"Average: %.1f\\n\", (float)sum / size);\n    printf(\"Highest: %d\\n\", max);\n    return 0;\n}", "hint": "sizeof(arr)/sizeof(arr[0]) gets length. += to sum. > to find max.", "rubric": "Average calculated correctly. Max is 95."},
        {"title": "String Functions", "theory": "## Strings in C\nStrings are character arrays ending with `\\0` (null terminator).\n\n```c\n#include <string.h>\n\nchar name[] = \"Alice\";\nprintf(\"%lu\\n\", strlen(name));       // 5\n\nchar dest[20];\nstrcpy(dest, \"Hello\");               // Copy\nstrcat(dest, \" World\");              // Concatenate\nprintf(\"%d\\n\", strcmp(\"abc\", \"def\")); // Compare (<0)\n```", "instructions": "## Task: String Utilities\n1. Create a string and find its length\n2. Copy it to another string\n3. Concatenate two strings\n4. Compare two strings", "starterCode": "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char greeting[50] = \"Hello\";\n    char name[] = \"Mabel\";\n    char full[50];\n    \n    printf(\"Length: %lu\\n\", ___(greeting));\n    \n    ___(full, greeting);     // Copy greeting to full\n    ___(full, \" \");          // Add space\n    ___(full, name);          // Add name\n    \n    printf(\"Full: %s\\n\", full);\n    printf(\"Equal? %d\\n\", ___(greeting, \"Hello\") == 0);\n    return 0;\n}", "solution": "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char greeting[50] = \"Hello\";\n    char name[] = \"Mabel\";\n    char full[50];\n    \n    printf(\"Length: %lu\\n\", strlen(greeting));\n    \n    strcpy(full, greeting);     // Copy greeting to full\n    strcat(full, \" \");          // Add space\n    strcat(full, name);          // Add name\n    \n    printf(\"Full: %s\\n\", full);\n    printf(\"Equal? %d\\n\", strcmp(greeting, \"Hello\") == 0);\n    return 0;\n}", "hint": "strlen for length. strcpy copies. strcat concatenates. strcmp compares (0 = equal).", "rubric": "Length is 5. Full is 'Hello Mabel'. Comparison returns true (1)."},
        {"title": "2D Arrays", "theory": "## Multidimensional Arrays\n\n```c\nint matrix[3][3] = {\n    {1, 2, 3},\n    {4, 5, 6},\n    {7, 8, 9}\n};\n\n// Access: matrix[row][col]\nprintf(\"%d\\n\", matrix[1][2]);  // 6\n\n// Iterate\nfor (int i = 0; i < 3; i++) {\n    for (int j = 0; j < 3; j++) {\n        printf(\"%d \", matrix[i][j]);\n    }\n    printf(\"\\n\");\n}\n```", "instructions": "## Task: Matrix Sum\n1. Create a 3x3 matrix\n2. Calculate and print the sum of all elements\n3. Calculate and print the sum of the main diagonal", "starterCode": "#include <stdio.h>\n\nint main() {\n    int matrix[3][3] = {\n        {1, 2, 3},\n        {4, 5, 6},\n        {7, 8, 9}\n    };\n    int total = 0, diagonal = 0;\n    \n    for (int i = 0; i < 3; i++) {\n        for (int j = 0; j < 3; j++) {\n            total ___ matrix[i][j];\n        }\n        diagonal ___ matrix[i][___];\n    }\n    \n    printf(\"Total sum: %d\\n\", total);\n    printf(\"Diagonal sum: %d\\n\", diagonal);\n    return 0;\n}", "solution": "#include <stdio.h>\n\nint main() {\n    int matrix[3][3] = {\n        {1, 2, 3},\n        {4, 5, 6},\n        {7, 8, 9}\n    };\n    int total = 0, diagonal = 0;\n    \n    for (int i = 0; i < 3; i++) {\n        for (int j = 0; j < 3; j++) {\n            total += matrix[i][j];\n        }\n        diagonal += matrix[i][i];\n    }\n    \n    printf(\"Total sum: %d\\n\", total);\n    printf(\"Diagonal sum: %d\\n\", diagonal);\n    return 0;\n}", "hint": "+= to accumulate. matrix[i][i] gives diagonal elements (0,0), (1,1), (2,2).", "rubric": "Total sum is 45. Diagonal sum is 15 (1+5+9)."},
        {"title": "String Manipulation", "theory": "## Manual String Operations\nSince C doesn't have built-in string methods like Python, you often write your own.\n\n```c\n// Convert to uppercase manually\nvoid to_upper(char *str) {\n    while (*str) {\n        if (*str >= 'a' && *str <= 'z')\n            *str -= 32;  // ASCII difference\n        str++;\n    }\n}\n```", "instructions": "## Task: Custom String Functions\n1. Write `int count_vowels(char *str)` that counts vowels\n2. Write `void reverse_string(char *str)` that reverses in-place\n3. Test both", "starterCode": "#include <stdio.h>\n#include <string.h>\n\nint count_vowels(char *str) {\n    int count = 0;\n    while (*str) {\n        char c = *str;\n        if (c=='a'||c=='e'||c=='i'||c=='o'||c=='u'||\n            c=='A'||c=='E'||c=='I'||c=='O'||c=='U')\n            count___;\n        str___;\n    }\n    return count;\n}\n\nvoid reverse_string(char *str) {\n    int len = strlen(str);\n    for (int i = 0; i < len/2; i++) {\n        char temp = str[i];\n        str[i] = str[len-1-___];\n        str[len-1-i] = ___;\n    }\n}\n\nint main() {\n    char text[] = \"Hello World\";\n    printf(\"Vowels: %d\\n\", count_vowels(text));\n    reverse_string(text);\n    printf(\"Reversed: %s\\n\", text);\n    return 0;\n}", "solution": "#include <stdio.h>\n#include <string.h>\n\nint count_vowels(char *str) {\n    int count = 0;\n    while (*str) {\n        char c = *str;\n        if (c=='a'||c=='e'||c=='i'||c=='o'||c=='u'||\n            c=='A'||c=='E'||c=='I'||c=='O'||c=='U')\n            count++;\n        str++;\n    }\n    return count;\n}\n\nvoid reverse_string(char *str) {\n    int len = strlen(str);\n    for (int i = 0; i < len/2; i++) {\n        char temp = str[i];\n        str[i] = str[len-1-i];\n        str[len-1-i] = temp;\n    }\n}\n\nint main() {\n    char text[] = \"Hello World\";\n    printf(\"Vowels: %d\\n\", count_vowels(text));\n    reverse_string(text);\n    printf(\"Reversed: %s\\n\", text);\n    return 0;\n}", "hint": "++ to increment count and pointer. Swap str[i] with str[len-1-i].", "rubric": "Vowels: 3 (e, o, o). Reversed: dlroW olleH."},
        {"title": "Command Line Arguments", "theory": "## argc and argv\n\n```c\nint main(int argc, char *argv[]) {\n    printf(\"Arguments: %d\\n\", argc);\n    for (int i = 0; i < argc; i++) {\n        printf(\"argv[%d] = %s\\n\", i, argv[i]);\n    }\n    return 0;\n}\n```\n\n- `argc` = argument count\n- `argv[0]` = program name\n- `argv[1]` = first argument, etc.", "instructions": "## Task: Argument Parser\n1. Simulate command-line argument parsing\n2. Parse key=value pairs from a string array\n3. Print each key and value", "starterCode": "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    // Simulated arguments\n    char *args[] = {\"./app\", \"--name=Mabel\", \"--port=3000\", \"--debug=true\"};\n    int argc = 4;\n    \n    for (int i = 1; i < ___; i++) {\n        char *eq = strchr(args[i], '=');\n        if (eq != ___) {\n            int key_len = eq - args[i];\n            printf(\"Key: %.*s, Value: %s\\n\", key_len, args[i], eq + 1);\n        }\n    }\n    return 0;\n}", "solution": "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    // Simulated arguments\n    char *args[] = {\"./app\", \"--name=Mabel\", \"--port=3000\", \"--debug=true\"};\n    int argc = 4;\n    \n    for (int i = 1; i < argc; i++) {\n        char *eq = strchr(args[i], '=');\n        if (eq != NULL) {\n            int key_len = eq - args[i];\n            printf(\"Key: %.*s, Value: %s\\n\", key_len, args[i], eq + 1);\n        }\n    }\n    return 0;\n}", "hint": "strchr finds '=' character. eq+1 points to value after '='. eq-args[i] gives key length.", "rubric": "All 3 key-value pairs parsed correctly."}
    ],
    # ─── DATA STRUCTURES & ALGORITHMS ───
    "Big O Notation": [
        {"title": "Time Complexity Basics", "theory": "## Big O Notation\nBig O describes how an algorithm's runtime grows as input size increases.\n\n```\nO(1)     — Constant    (array access)\nO(log n) — Logarithmic (binary search)\nO(n)     — Linear      (loop through array)\nO(n log n) — Log-linear (merge sort)\nO(n^2)   — Quadratic   (nested loops)\nO(2^n)   — Exponential (recursive fibonacci)\n```\n\nWe always focus on the **worst case** and ignore constants.", "instructions": "## Task: Identify Time Complexity\n1. Create functions with different time complexities\n2. Label each with its Big O\n3. Compare them by running with different input sizes", "starterCode": "import time\n\n# O(1) - Constant\ndef get_first(arr):\n    return arr[0] if arr else None\n\n# O(n) - Linear\ndef find_max(arr):\n    m = arr[0]\n    for x in arr:\n        if x ___ m: m = x\n    return m\n\n# O(n^2) - Quadratic\ndef has_duplicate(arr):\n    for i in range(len(arr)):\n        for j in range(i+1, len(arr)):\n            if arr[i] ___ arr[j]: return True\n    return False\n\narr = list(range(1000))\nprint(f'First: {get_first(arr)}')\nprint(f'Max: {find_max(arr)}')\nprint(f'Has dup: {has_duplicate(arr)}')", "solution": "import time\n\n# O(1) - Constant\ndef get_first(arr):\n    return arr[0] if arr else None\n\n# O(n) - Linear\ndef find_max(arr):\n    m = arr[0]\n    for x in arr:\n        if x > m: m = x\n    return m\n\n# O(n^2) - Quadratic\ndef has_duplicate(arr):\n    for i in range(len(arr)):\n        for j in range(i+1, len(arr)):\n            if arr[i] == arr[j]: return True\n    return False\n\narr = list(range(1000))\nprint(f'First: {get_first(arr)}')\nprint(f'Max: {find_max(arr)}')\nprint(f'Has dup: {has_duplicate(arr)}')", "hint": "> for finding max. == for comparing duplicates.", "rubric": "Functions implement correct complexities. First: 0, Max: 999, Has dup: False."},
        {"title": "Space Complexity", "theory": "## Space Complexity\nHow much extra memory does your algorithm use?\n\n```python\n# O(1) space - uses fixed extra memory\ndef sum_array(arr):\n    total = 0\n    for x in arr:\n        total += x\n    return total\n\n# O(n) space - creates new array\ndef double_array(arr):\n    result = []\n    for x in arr:\n        result.append(x * 2)\n    return result\n\n# O(n) space - recursive call stack\ndef factorial(n):\n    if n <= 1: return 1\n    return n * factorial(n - 1)\n```", "instructions": "## Task: Space-Efficient Reverse\n1. Write `reverse_in_place(arr)` that reverses using O(1) extra space\n2. Write `reverse_new(arr)` that creates a new reversed array O(n) space\n3. Compare both approaches", "starterCode": "# O(1) space - swap in place\ndef reverse_in_place(arr):\n    left, right = 0, len(arr) - 1\n    while left ___ right:\n        arr[left], arr[right] = arr[right], arr[left]\n        left ___ 1\n        right ___ 1\n    return arr\n\n# O(n) space - new array\ndef reverse_new(arr):\n    return arr[___]\n\narr1 = [1, 2, 3, 4, 5]\narr2 = arr1.copy()\nprint(f'In-place: {reverse_in_place(arr1)}')\nprint(f'New array: {reverse_new(arr2)}')", "solution": "# O(1) space - swap in place\ndef reverse_in_place(arr):\n    left, right = 0, len(arr) - 1\n    while left < right:\n        arr[left], arr[right] = arr[right], arr[left]\n        left += 1\n        right -= 1\n    return arr\n\n# O(n) space - new array\ndef reverse_new(arr):\n    return arr[::-1]\n\narr1 = [1, 2, 3, 4, 5]\narr2 = arr1.copy()\nprint(f'In-place: {reverse_in_place(arr1)}')\nprint(f'New array: {reverse_new(arr2)}')", "hint": "left < right to converge. += 1 and -= 1 to move inward. [::-1] reverses.", "rubric": "Both produce [5, 4, 3, 2, 1]. In-place uses O(1) space."},
        {"title": "Comparing Algorithms", "theory": "## Choosing the Right Algorithm\n\n| Input Size | O(n) | O(n log n) | O(n^2) |\n|-----------|------|-----------|--------|\n| 10 | 10 | 33 | 100 |\n| 100 | 100 | 664 | 10,000 |\n| 1,000 | 1,000 | 9,966 | 1,000,000 |\n| 1,000,000 | 1M | 20M | 1 TRILLION |\n\nAt scale, algorithm choice matters enormously!", "instructions": "## Task: Benchmark Comparison\n1. Implement linear search O(n) and binary search O(log n)\n2. Time both on a sorted array of 100,000 elements\n3. Print the time difference", "starterCode": "import time\n\ndef linear_search(arr, target):\n    for i in range(len(arr)):\n        if arr[i] == target:\n            return i\n    return -1\n\ndef binary_search(arr, target):\n    low, high = 0, len(arr) - 1\n    while low ___ high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] ___ target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n\narr = list(range(100000))\ntarget = 99999\n\nstart = time.time()\nlinear_search(arr, target)\nlinear_time = time.time() - start\n\nstart = time.time()\nbinary_search(arr, target)\nbinary_time = time.time() - start\n\nprint(f'Linear: {linear_time:.6f}s')\nprint(f'Binary: {binary_time:.6f}s')\nprint(f'Binary is {linear_time/max(binary_time,0.000001):.0f}x faster')", "solution": "import time\n\ndef linear_search(arr, target):\n    for i in range(len(arr)):\n        if arr[i] == target:\n            return i\n    return -1\n\ndef binary_search(arr, target):\n    low, high = 0, len(arr) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n\narr = list(range(100000))\ntarget = 99999\n\nstart = time.time()\nlinear_search(arr, target)\nlinear_time = time.time() - start\n\nstart = time.time()\nbinary_search(arr, target)\nbinary_time = time.time() - start\n\nprint(f'Linear: {linear_time:.6f}s')\nprint(f'Binary: {binary_time:.6f}s')\nprint(f'Binary is {linear_time/max(binary_time,0.000001):.0f}x faster')", "hint": "<= for while condition. < target means search right half (low = mid + 1).", "rubric": "Both find the target. Binary search is significantly faster."},
        {"title": "Best/Average/Worst Case", "theory": "## Cases Matter\n\n```python\ndef linear_search(arr, target):\n    for i, x in enumerate(arr):\n        if x == target:\n            return i\n    return -1\n```\n\n- **Best case:** O(1) — target is first element\n- **Average case:** O(n/2) = O(n) — target is in the middle\n- **Worst case:** O(n) — target is last or not present\n\nWe usually talk about **worst case** unless specified.", "instructions": "## Task: Analyze Cases\n1. Create a search function that counts comparisons\n2. Test with best case (first element), average (middle), worst (last)\n3. Print the number of comparisons for each", "starterCode": "def search_with_count(arr, target):\n    comparisons = 0\n    for i in range(len(arr)):\n        comparisons ___ 1\n        if arr[i] == target:\n            return i, comparisons\n    return -1, comparisons\n\narr = list(range(1000))\n\nbest_idx, best_comp = search_with_count(arr, ___)\navg_idx, avg_comp = search_with_count(arr, ___)\nworst_idx, worst_comp = search_with_count(arr, ___)\n\nprint(f'Best case: {best_comp} comparisons')\nprint(f'Average case: {avg_comp} comparisons')\nprint(f'Worst case: {worst_comp} comparisons')", "solution": "def search_with_count(arr, target):\n    comparisons = 0\n    for i in range(len(arr)):\n        comparisons += 1\n        if arr[i] == target:\n            return i, comparisons\n    return -1, comparisons\n\narr = list(range(1000))\n\nbest_idx, best_comp = search_with_count(arr, 0)\navg_idx, avg_comp = search_with_count(arr, 500)\nworst_idx, worst_comp = search_with_count(arr, 999)\n\nprint(f'Best case: {best_comp} comparisons')\nprint(f'Average case: {avg_comp} comparisons')\nprint(f'Worst case: {worst_comp} comparisons')", "hint": "+= 1 counts. 0 for best (first), 500 for average, 999 for worst (last).", "rubric": "Best: 1 comparison. Average: 501. Worst: 1000."},
        {"title": "Amortized Analysis", "theory": "## Amortized Time\nSome operations are expensive occasionally but cheap on average.\n\n```python\n# Python list.append() is O(1) amortized\n# Usually O(1), but occasionally O(n) when resizing\narr = []\nfor i in range(1000000):\n    arr.append(i)  # Total time: O(n), so each append is O(1) amortized\n```\n\nAmortized = total cost / number of operations", "instructions": "## Task: Dynamic Array\n1. Implement a simplified dynamic array that doubles capacity when full\n2. Track the number of copy operations during resizing\n3. Show that amortized cost per insert is O(1)", "starterCode": "class DynamicArray:\n    def __init__(self):\n        self.data = [None] * 2\n        self.size = 0\n        self.capacity = 2\n        self.total_copies = 0\n    \n    def append(self, value):\n        if self.size == self.capacity:\n            self._resize()\n        self.data[self.___] = value\n        self.size ___ 1\n    \n    def _resize(self):\n        new_cap = self.capacity * ___\n        new_data = [None] * new_cap\n        for i in range(self.size):\n            new_data[i] = self.data[i]\n            self.total_copies += 1\n        self.data = new_data\n        self.capacity = new_cap\n\narr = DynamicArray()\nfor i in range(100):\n    arr.append(i)\n\nprint(f'Size: {arr.size}')\nprint(f'Capacity: {arr.capacity}')\nprint(f'Total copies: {arr.total_copies}')\nprint(f'Amortized copies per insert: {arr.total_copies / arr.size:.2f}')", "solution": "class DynamicArray:\n    def __init__(self):\n        self.data = [None] * 2\n        self.size = 0\n        self.capacity = 2\n        self.total_copies = 0\n    \n    def append(self, value):\n        if self.size == self.capacity:\n            self._resize()\n        self.data[self.size] = value\n        self.size += 1\n    \n    def _resize(self):\n        new_cap = self.capacity * 2\n        new_data = [None] * new_cap\n        for i in range(self.size):\n            new_data[i] = self.data[i]\n            self.total_copies += 1\n        self.data = new_data\n        self.capacity = new_cap\n\narr = DynamicArray()\nfor i in range(100):\n    arr.append(i)\n\nprint(f'Size: {arr.size}')\nprint(f'Capacity: {arr.capacity}')\nprint(f'Total copies: {arr.total_copies}')\nprint(f'Amortized copies per insert: {arr.total_copies / arr.size:.2f}')", "hint": "self.size for next empty index. += 1 to grow. * 2 to double capacity.", "rubric": "Array grows dynamically. Amortized copies per insert is close to 1."}
    ],
    "Linked Lists": [
        {"title": "Singly Linked List", "theory": "## Linked Lists\nA linked list stores elements in nodes, where each node points to the next.\n\n```python\nclass Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n```\n\nUnlike arrays, linked lists can grow without resizing and insert/delete in O(1).", "instructions": "## Task: Build a Linked List\n1. Create Node and LinkedList classes\n2. Implement `append(data)`, `display()`, and `length()`\n3. Test with 5 values", "starterCode": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = ___\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        new_node = Node(data)\n        if self.head is None:\n            self.head = new_node\n            return\n        current = self.head\n        while current.___ is not None:\n            current = current.next\n        current.next = ___\n    \n    def display(self):\n        items = []\n        current = self.head\n        while current:\n            items.append(str(current.data))\n            current = current.next\n        print(' -> '.join(items))\n    \n    def length(self):\n        count = 0\n        current = self.head\n        while current:\n            count += 1\n            current = current.___\n        return count\n\nll = LinkedList()\nfor v in [10, 20, 30, 40, 50]:\n    ll.append(v)\nll.display()\nprint(f'Length: {ll.length()}')", "solution": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        new_node = Node(data)\n        if self.head is None:\n            self.head = new_node\n            return\n        current = self.head\n        while current.next is not None:\n            current = current.next\n        current.next = new_node\n    \n    def display(self):\n        items = []\n        current = self.head\n        while current:\n            items.append(str(current.data))\n            current = current.next\n        print(' -> '.join(items))\n    \n    def length(self):\n        count = 0\n        current = self.head\n        while current:\n            count += 1\n            current = current.next\n        return count\n\nll = LinkedList()\nfor v in [10, 20, 30, 40, 50]:\n    ll.append(v)\nll.display()\nprint(f'Length: {ll.length()}')", "hint": "None for empty next. Traverse with while current.next. Set current.next = new_node.", "rubric": "Display: 10 -> 20 -> 30 -> 40 -> 50. Length: 5."},
        {"title": "Insert & Delete", "theory": "## Linked List Operations\n\n```python\n# Insert at beginning: O(1)\ndef prepend(self, data):\n    new_node = Node(data)\n    new_node.next = self.head\n    self.head = new_node\n\n# Delete by value: O(n)\ndef delete(self, data):\n    if self.head and self.head.data == data:\n        self.head = self.head.next\n        return\n    current = self.head\n    while current.next:\n        if current.next.data == data:\n            current.next = current.next.next\n            return\n        current = current.next\n```", "instructions": "## Task: Full Linked List Operations\n1. Add `prepend(data)` to insert at the beginning\n2. Add `delete(data)` to remove by value\n3. Add `insert_at(index, data)` to insert at a position", "starterCode": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        node = Node(data)\n        if not self.head: self.head = node; return\n        cur = self.head\n        while cur.next: cur = cur.next\n        cur.next = node\n    \n    def prepend(self, data):\n        node = Node(data)\n        node.next = self.___\n        self.head = ___\n    \n    def delete(self, data):\n        if self.head and self.head.data == data:\n            self.head = self.head.___\n            return\n        cur = self.head\n        while cur and cur.next:\n            if cur.next.data == data:\n                cur.next = cur.next.___\n                return\n            cur = cur.next\n    \n    def display(self):\n        items, cur = [], self.head\n        while cur: items.append(str(cur.data)); cur = cur.next\n        print(' -> '.join(items) if items else 'Empty')\n\nll = LinkedList()\nfor v in [20, 30, 40]: ll.append(v)\nll.prepend(10)\nll.display()\nll.delete(30)\nll.display()", "solution": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        node = Node(data)\n        if not self.head: self.head = node; return\n        cur = self.head\n        while cur.next: cur = cur.next\n        cur.next = node\n    \n    def prepend(self, data):\n        node = Node(data)\n        node.next = self.head\n        self.head = node\n    \n    def delete(self, data):\n        if self.head and self.head.data == data:\n            self.head = self.head.next\n            return\n        cur = self.head\n        while cur and cur.next:\n            if cur.next.data == data:\n                cur.next = cur.next.next\n                return\n            cur = cur.next\n    \n    def display(self):\n        items, cur = [], self.head\n        while cur: items.append(str(cur.data)); cur = cur.next\n        print(' -> '.join(items) if items else 'Empty')\n\nll = LinkedList()\nfor v in [20, 30, 40]: ll.append(v)\nll.prepend(10)\nll.display()\nll.delete(30)\nll.display()", "hint": "Prepend: point new node to old head, update head. Delete: skip the node.", "rubric": "After prepend: 10->20->30->40. After delete 30: 10->20->40."},
        {"title": "Reverse a Linked List", "theory": "## Classic Interview Question\nReversing a linked list is one of the most common interview questions.\n\n```\nBefore: 1 -> 2 -> 3 -> 4 -> None\nAfter:  4 -> 3 -> 2 -> 1 -> None\n```\n\nUse three pointers: `prev`, `current`, `next`.", "instructions": "## Task: Reverse Linked List\n1. Implement `reverse()` method using iterative approach\n2. Use three pointers: prev, current, next_node\n3. Test by displaying before and after", "starterCode": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        node = Node(data)\n        if not self.head: self.head = node; return\n        cur = self.head\n        while cur.next: cur = cur.next\n        cur.next = node\n    \n    def reverse(self):\n        prev = ___\n        current = self.head\n        while current:\n            next_node = current.___\n            current.next = ___\n            prev = ___\n            current = next_node\n        self.head = ___\n    \n    def display(self):\n        items, cur = [], self.head\n        while cur: items.append(str(cur.data)); cur = cur.next\n        print(' -> '.join(items))\n\nll = LinkedList()\nfor v in [1, 2, 3, 4, 5]: ll.append(v)\nprint('Before:'); ll.display()\nll.reverse()\nprint('After:'); ll.display()", "solution": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, data):\n        node = Node(data)\n        if not self.head: self.head = node; return\n        cur = self.head\n        while cur.next: cur = cur.next\n        cur.next = node\n    \n    def reverse(self):\n        prev = None\n        current = self.head\n        while current:\n            next_node = current.next\n            current.next = prev\n            prev = current\n            current = next_node\n        self.head = prev\n    \n    def display(self):\n        items, cur = [], self.head\n        while cur: items.append(str(cur.data)); cur = cur.next\n        print(' -> '.join(items))\n\nll = LinkedList()\nfor v in [1, 2, 3, 4, 5]: ll.append(v)\nprint('Before:'); ll.display()\nll.reverse()\nprint('After:'); ll.display()", "hint": "prev starts None. Save next, point current back to prev, advance both.", "rubric": "Before: 1->2->3->4->5. After: 5->4->3->2->1."},
        {"title": "Detect Cycle", "theory": "## Floyd's Cycle Detection\nUse two pointers (slow and fast) to detect cycles.\n\n```\nIf fast catches up to slow, there's a cycle!\n\nSlow moves 1 step at a time.\nFast moves 2 steps at a time.\n```\n\nThis is called the **tortoise and hare** algorithm.", "instructions": "## Task: Cycle Detection\n1. Create a linked list\n2. Implement `has_cycle()` using Floyd's algorithm\n3. Test with a normal list and one with a cycle", "starterCode": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\ndef has_cycle(head):\n    slow = head\n    fast = head\n    while fast and fast.next:\n        slow = slow.___\n        fast = fast.next.___\n        if slow ___ fast:\n            return True\n    return False\n\n# No cycle\na, b, c = Node(1), Node(2), Node(3)\na.next = b; b.next = c\nprint(f'Has cycle: {has_cycle(a)}')\n\n# With cycle\nx, y, z = Node(1), Node(2), Node(3)\nx.next = y; y.next = z; z.next = x  # Cycle!\nprint(f'Has cycle: {has_cycle(x)}')", "solution": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\ndef has_cycle(head):\n    slow = head\n    fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            return True\n    return False\n\n# No cycle\na, b, c = Node(1), Node(2), Node(3)\na.next = b; b.next = c\nprint(f'Has cycle: {has_cycle(a)}')\n\n# With cycle\nx, y, z = Node(1), Node(2), Node(3)\nx.next = y; y.next = z; z.next = x  # Cycle!\nprint(f'Has cycle: {has_cycle(x)}')", "hint": "slow.next moves 1 step. fast.next.next moves 2 steps. == to check if they meet.", "rubric": "No cycle: False. With cycle: True. Floyd's algorithm implemented."},
        {"title": "Merge Two Sorted Lists", "theory": "## Merging Sorted Lists\nAnother classic interview question: merge two sorted linked lists into one.\n\n```\nList A: 1 -> 3 -> 5\nList B: 2 -> 4 -> 6\nResult: 1 -> 2 -> 3 -> 4 -> 5 -> 6\n```\n\nCompare heads, take the smaller one, advance that pointer.", "instructions": "## Task: Merge Sorted Lists\n1. Create two sorted linked lists\n2. Merge them into a single sorted list\n3. Display the result", "starterCode": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\ndef merge_sorted(h1, h2):\n    dummy = Node(0)\n    current = dummy\n    while h1 and h2:\n        if h1.data ___ h2.data:\n            current.next = ___\n            h1 = h1.next\n        else:\n            current.next = ___\n            h2 = h2.next\n        current = current.next\n    current.next = h1 if h1 else h2\n    return dummy.next\n\ndef display(head):\n    items = []\n    while head: items.append(str(head.data)); head = head.next\n    print(' -> '.join(items))\n\n# Create sorted lists\na1, a2, a3 = Node(1), Node(3), Node(5)\na1.next = a2; a2.next = a3\nb1, b2, b3 = Node(2), Node(4), Node(6)\nb1.next = b2; b2.next = b3\n\nresult = merge_sorted(a1, b1)\ndisplay(result)", "solution": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\ndef merge_sorted(h1, h2):\n    dummy = Node(0)\n    current = dummy\n    while h1 and h2:\n        if h1.data <= h2.data:\n            current.next = h1\n            h1 = h1.next\n        else:\n            current.next = h2\n            h2 = h2.next\n        current = current.next\n    current.next = h1 if h1 else h2\n    return dummy.next\n\ndef display(head):\n    items = []\n    while head: items.append(str(head.data)); head = head.next\n    print(' -> '.join(items))\n\n# Create sorted lists\na1, a2, a3 = Node(1), Node(3), Node(5)\na1.next = a2; a2.next = a3\nb1, b2, b3 = Node(2), Node(4), Node(6)\nb1.next = b2; b2.next = b3\n\nresult = merge_sorted(a1, b1)\ndisplay(result)", "hint": "<= takes the smaller. Attach the remaining list at the end.", "rubric": "Output: 1 -> 2 -> 3 -> 4 -> 5 -> 6. Sorted correctly."}
    ],
    # ─── DATA SCIENCE ───
    "Pandas Intro": [
        {"title": "Creating DataFrames", "theory": "## Pandas DataFrames\nA DataFrame is a 2D table (like a spreadsheet) for data analysis.\n\n```python\nimport pandas as pd\n\n# From dictionary\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie'],\n    'Age': [25, 30, 35],\n    'City': ['NYC', 'LA', 'Chicago']\n})\n\nprint(df)\nprint(df.shape)    # (3, 3) rows, cols\nprint(df.columns)  # Index(['Name', 'Age', 'City'])\n```", "instructions": "## Task: Student DataFrame\n1. Create a DataFrame with columns: Name, Grade, Score\n2. Add 5 students\n3. Print the shape, columns, and first 3 rows", "starterCode": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Grade': ['A', 'B', 'A', 'C', 'B'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(df.___)\nprint(df.___)\nprint(df.___(3))", "solution": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Grade': ['A', 'B', 'A', 'C', 'B'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(df.shape)\nprint(df.columns)\nprint(df.head(3))", "hint": ".shape for dimensions. .columns for column names. .head(n) for first n rows.", "rubric": "Shape: (5, 3). Columns listed. First 3 rows displayed."},
        {"title": "Selecting Data", "theory": "## Accessing DataFrame Data\n\n```python\n# Select column\ndf['Name']            # Series\ndf[['Name', 'Age']]   # DataFrame (multiple cols)\n\n# Select rows by condition\ndf[df['Age'] > 25]\n\n# Select by index\ndf.iloc[0]      # First row\ndf.iloc[1:3]    # Rows 1-2\ndf.loc[0, 'Name']  # Specific cell\n```", "instructions": "## Task: Filter Students\n1. Select only the 'Name' and 'Score' columns\n2. Filter students with Score > 85\n3. Get the student in the last row using iloc", "starterCode": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(df[['___', '___']])\nprint(df[df['Score'] ___ 85])\nprint(df.___[-1])", "solution": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(df[['Name', 'Score']])\nprint(df[df['Score'] > 85])\nprint(df.iloc[-1])", "hint": "Double brackets for multiple columns. > for filtering. iloc[-1] for last row.", "rubric": "Two columns selected. Filter shows Alice and Charlie. Last row is Eve."},
        {"title": "Basic Statistics", "theory": "## Descriptive Statistics\n\n```python\ndf['Score'].mean()    # Average\ndf['Score'].median()  # Middle value\ndf['Score'].std()     # Standard deviation\ndf['Score'].min()     # Minimum\ndf['Score'].max()     # Maximum\n\n# All at once\ndf.describe()\n\n# Value counts\ndf['Grade'].value_counts()\n```", "instructions": "## Task: Score Analysis\n1. Calculate mean, median, min, max of scores\n2. Use describe() for full summary\n3. Count how many students got each grade", "starterCode": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Grade': ['A', 'B', 'A', 'C', 'B'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(f\"Mean: {df['Score'].___():.1f}\")\nprint(f\"Median: {df['Score'].___()}\")\nprint(f\"Min: {df['Score'].___()}, Max: {df['Score'].___()}\")\nprint()\nprint(df.___()\nprint()\nprint(df['Grade'].___())", "solution": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],\n    'Grade': ['A', 'B', 'A', 'C', 'B'],\n    'Score': [95, 82, 91, 73, 85]\n})\n\nprint(f\"Mean: {df['Score'].mean():.1f}\")\nprint(f\"Median: {df['Score'].median()}\")\nprint(f\"Min: {df['Score'].min()}, Max: {df['Score'].max()}\")\nprint()\nprint(df.describe())\nprint()\nprint(df['Grade'].value_counts())", "hint": ".mean(), .median(), .min(), .max() for stats. .describe() for all. .value_counts() for frequency.", "rubric": "Mean: 85.2. All stats correct. Grade counts: A:2, B:2, C:1."},
        {"title": "Adding & Modifying Columns", "theory": "## Modifying DataFrames\n\n```python\n# Add new column\ndf['Bonus'] = df['Score'] * 0.1\n\n# Conditional column\ndf['Pass'] = df['Score'] >= 60\n\n# Apply function\ndf['Grade_Upper'] = df['Grade'].apply(lambda x: x.upper())\n\n# Rename columns\ndf.rename(columns={'Score': 'Test_Score'}, inplace=True)\n\n# Drop column\ndf.drop('Bonus', axis=1, inplace=True)\n```", "instructions": "## Task: Enhance Student Data\n1. Add a 'Percentage' column (Score out of 100, formatted)\n2. Add a 'Status' column: 'Pass' if Score >= 60, else 'Fail'\n3. Add a 'Letter' column using a function", "starterCode": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana'],\n    'Score': [95, 45, 72, 88]\n})\n\ndf['Percentage'] = df['Score'].apply(lambda x: f'{x}%')\ndf['Status'] = df['Score'].apply(lambda x: '___' if x >= 60 else '___')\n\ndef get_letter(score):\n    if score >= 90: return 'A'\n    elif score >= 80: return 'B'\n    elif score >= 70: return 'C'\n    elif score >= 60: return 'D'\n    else: return 'F'\n\ndf['Letter'] = df['Score'].___(get_letter)\nprint(df)", "solution": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Name': ['Alice', 'Bob', 'Charlie', 'Diana'],\n    'Score': [95, 45, 72, 88]\n})\n\ndf['Percentage'] = df['Score'].apply(lambda x: f'{x}%')\ndf['Status'] = df['Score'].apply(lambda x: 'Pass' if x >= 60 else 'Fail')\n\ndef get_letter(score):\n    if score >= 90: return 'A'\n    elif score >= 80: return 'B'\n    elif score >= 70: return 'C'\n    elif score >= 60: return 'D'\n    else: return 'F'\n\ndf['Letter'] = df['Score'].apply(get_letter)\nprint(df)", "hint": "lambda for inline. .apply(function) maps function to each value.", "rubric": "Percentage formatted. Pass/Fail correct. Letter grades match scores."},
        {"title": "GroupBy & Aggregation", "theory": "## Grouping Data\n\n```python\n# Group by category and aggregate\ndf.groupby('Department')['Salary'].mean()\n\n# Multiple aggregations\ndf.groupby('Department').agg({\n    'Salary': ['mean', 'max'],\n    'Age': 'mean'\n})\n\n# Group by multiple columns\ndf.groupby(['Department', 'Level'])['Salary'].mean()\n```", "instructions": "## Task: Sales Analysis\n1. Create a sales DataFrame with Region, Product, Amount\n2. Group by Region to find total sales per region\n3. Group by Product to find average sale amount", "starterCode": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Region': ['North', 'South', 'North', 'South', 'North', 'South'],\n    'Product': ['Laptop', 'Phone', 'Laptop', 'Laptop', 'Phone', 'Phone'],\n    'Amount': [1200, 800, 1100, 1300, 750, 850]\n})\n\nprint('Total by Region:')\nprint(df.groupby('___')['Amount'].___())\nprint()\nprint('Average by Product:')\nprint(df.groupby('___')['Amount'].___())", "solution": "import pandas as pd\n\ndf = pd.DataFrame({\n    'Region': ['North', 'South', 'North', 'South', 'North', 'South'],\n    'Product': ['Laptop', 'Phone', 'Laptop', 'Laptop', 'Phone', 'Phone'],\n    'Amount': [1200, 800, 1100, 1300, 750, 850]\n})\n\nprint('Total by Region:')\nprint(df.groupby('Region')['Amount'].sum())\nprint()\nprint('Average by Product:')\nprint(df.groupby('Product')['Amount'].mean())", "hint": "groupby('col') groups rows. .sum() for total, .mean() for average.", "rubric": "Regional totals correct. Product averages calculated."}
    ],
    # ─── AI ENGINEERING ───
    "Intro to LLMs": [
        {"title": "What are LLMs?", "theory": "## Large Language Models\nLLMs are AI models trained on massive text data to understand and generate language.\n\n```\nInput:  'The capital of France is'\nOutput: 'Paris'\n```\n\nKey concepts:\n- **Tokens**: Words/subwords the model processes\n- **Context Window**: How much text the model can see at once\n- **Temperature**: Controls randomness (0 = deterministic, 1 = creative)\n- **Prompt**: Your input to the model", "instructions": "## Task: Token Counter\n1. Create a simple tokenizer that splits text into tokens\n2. Count the tokens\n3. Estimate API cost based on token count", "starterCode": "def simple_tokenize(text):\n    # Rough approximation: ~4 chars per token\n    words = text.split()\n    token_count = 0\n    for word in words:\n        token_count += max(1, len(word) // ___)\n    return token_count\n\ndef estimate_cost(tokens, price_per_1k=0.002):\n    return (tokens / ___) * price_per_1k\n\ntext = 'Large language models are transforming how we interact with technology and build applications'\ntokens = simple_tokenize(text)\nprint(f'Text: {text}')\nprint(f'Estimated tokens: {tokens}')\nprint(f'Estimated cost: ${estimate_cost(tokens):.6f}')", "solution": "def simple_tokenize(text):\n    # Rough approximation: ~4 chars per token\n    words = text.split()\n    token_count = 0\n    for word in words:\n        token_count += max(1, len(word) // 4)\n    return token_count\n\ndef estimate_cost(tokens, price_per_1k=0.002):\n    return (tokens / 1000) * price_per_1k\n\ntext = 'Large language models are transforming how we interact with technology and build applications'\ntokens = simple_tokenize(text)\nprint(f'Text: {text}')\nprint(f'Estimated tokens: {tokens}')\nprint(f'Estimated cost: ${estimate_cost(tokens):.6f}')", "hint": "~4 chars per token. Divide tokens by 1000 for pricing.", "rubric": "Token count estimated. Cost calculated based on token count."},
        {"title": "Temperature & Sampling", "theory": "## How LLMs Generate Text\nLLMs predict the next token by assigning probabilities.\n\n```\n'The cat sat on the' -> \n  'mat'  (40%)\n  'floor' (25%)\n  'roof'  (15%)\n  'dog'   (5%)\n  ...\n```\n\n**Temperature** controls randomness:\n- `temp=0`: Always picks highest probability (deterministic)\n- `temp=0.7`: Good balance of creativity and coherence\n- `temp=1.5`: Very creative but might be incoherent", "instructions": "## Task: Simulate Text Generation\n1. Create a probability distribution for next words\n2. Implement temperature-based sampling\n3. Show how temperature affects word selection", "starterCode": "import random\nimport math\n\ndef apply_temperature(probs, temperature):\n    if temperature == 0:\n        return [1.0 if p == max(probs) else 0.0 for p in probs]\n    scaled = [p ** (1.0 / ___) for p in probs]\n    total = sum(scaled)\n    return [p / ___ for p in scaled]\n\ndef sample(words, probs, temperature=1.0):\n    adjusted = apply_temperature(probs, temperature)\n    r = random.random()\n    cumulative = 0\n    for word, prob in zip(words, adjusted):\n        cumulative += prob\n        if r <= cumulative:\n            return word\n    return words[-1]\n\nwords = ['mat', 'floor', 'roof', 'table', 'chair']\nprobs = [0.4, 0.25, 0.15, 0.12, 0.08]\n\nfor temp in [0, 0.5, 1.0, 1.5]:\n    results = [sample(words, probs, temp) for _ in range(10)]\n    print(f'Temp {temp}: {results}')", "solution": "import random\nimport math\n\ndef apply_temperature(probs, temperature):\n    if temperature == 0:\n        return [1.0 if p == max(probs) else 0.0 for p in probs]\n    scaled = [p ** (1.0 / temperature) for p in probs]\n    total = sum(scaled)\n    return [p / total for p in scaled]\n\ndef sample(words, probs, temperature=1.0):\n    adjusted = apply_temperature(probs, temperature)\n    r = random.random()\n    cumulative = 0\n    for word, prob in zip(words, adjusted):\n        cumulative += prob\n        if r <= cumulative:\n            return word\n    return words[-1]\n\nwords = ['mat', 'floor', 'roof', 'table', 'chair']\nprobs = [0.4, 0.25, 0.15, 0.12, 0.08]\n\nfor temp in [0, 0.5, 1.0, 1.5]:\n    results = [sample(words, probs, temp) for _ in range(10)]\n    print(f'Temp {temp}: {results}')", "hint": "1/temperature scales probabilities. Divide by total to normalize.", "rubric": "Temp 0 always picks 'mat'. Higher temps show more variety."},
        {"title": "Prompt Templates", "theory": "## Structured Prompts\nWell-structured prompts get better results from LLMs.\n\n```python\ntemplate = '''\nYou are a {role}.\n\nTask: {task}\n\nContext: {context}\n\nFormat your response as: {format}\n'''\n\nprompt = template.format(\n    role='senior Python developer',\n    task='Review this code for bugs',\n    context='def add(a, b): return a - b',\n    format='bullet points'\n)\n```", "instructions": "## Task: Prompt Template System\n1. Create a PromptTemplate class that stores templates with {placeholders}\n2. Implement a `fill(**kwargs)` method to substitute values\n3. Add validation for missing placeholders", "starterCode": "import re\n\nclass PromptTemplate:\n    def __init__(self, template):\n        self.template = template\n        self.placeholders = re.findall(r'\\{(\\w+)\\}', template)\n    \n    def fill(self, **kwargs):\n        missing = [p for p in self.___ if p not in kwargs]\n        if missing:\n            raise ValueError(f'Missing: {missing}')\n        return self.template.format(**___)\n    \n    def __str__(self):\n        return f'Template with placeholders: {self.placeholders}'\n\ntemplate = PromptTemplate(\n    'You are a {role}. Help the user with {task}. Respond in {language}.'\n)\nprint(template)\nprint()\nresult = template.fill(role='tutor', task='learning Python', language='English')\nprint(result)", "solution": "import re\n\nclass PromptTemplate:\n    def __init__(self, template):\n        self.template = template\n        self.placeholders = re.findall(r'\\{(\\w+)\\}', template)\n    \n    def fill(self, **kwargs):\n        missing = [p for p in self.placeholders if p not in kwargs]\n        if missing:\n            raise ValueError(f'Missing: {missing}')\n        return self.template.format(**kwargs)\n    \n    def __str__(self):\n        return f'Template with placeholders: {self.placeholders}'\n\ntemplate = PromptTemplate(\n    'You are a {role}. Help the user with {task}. Respond in {language}.'\n)\nprint(template)\nprint()\nresult = template.fill(role='tutor', task='learning Python', language='English')\nprint(result)", "hint": "self.placeholders to check for missing. **kwargs passes all keyword args.", "rubric": "Placeholders extracted. Template filled. Missing values raise error."},
        {"title": "Chat History Management", "theory": "## Managing Conversations\nLLMs are stateless — you must send the full conversation history each time.\n\n```python\nmessages = [\n    {'role': 'system', 'content': 'You are a helpful assistant.'},\n    {'role': 'user', 'content': 'What is Python?'},\n    {'role': 'assistant', 'content': 'Python is a programming language...'},\n    {'role': 'user', 'content': 'How do I install it?'}\n]\n```\n\nRoles: `system` (instructions), `user` (input), `assistant` (model output)", "instructions": "## Task: Chat Manager\n1. Create a ChatHistory class that manages messages\n2. Track token count to stay within context window\n3. Implement message trimming when too long", "starterCode": "class ChatHistory:\n    def __init__(self, max_tokens=4000):\n        self.messages = []\n        self.max_tokens = max_tokens\n    \n    def add_system(self, content):\n        self.messages.append({'role': '___', 'content': content})\n    \n    def add_user(self, content):\n        self.messages.append({'role': '___', 'content': content})\n    \n    def add_assistant(self, content):\n        self.messages.append({'role': '___', 'content': content})\n    \n    def estimate_tokens(self):\n        return sum(len(m['content']) // 4 for m in self.messages)\n    \n    def get_messages(self):\n        # Trim old messages if over limit (keep system + recent)\n        while self.estimate_tokens() > self.max_tokens and len(self.messages) > 2:\n            self.messages.pop(1)  # Remove oldest non-system message\n        return self.messages\n    \n    def display(self):\n        for m in self.messages:\n            print(f\"[{m['role']}]: {m['content'][:50]}...\")\n\nchat = ChatHistory(max_tokens=100)\nchat.add_system('You are a helpful tutor.')\nchat.add_user('What is Python?')\nchat.add_assistant('Python is a high-level programming language.')\nchat.add_user('How do I learn it?')\nchat.display()\nprint(f'Tokens: ~{chat.estimate_tokens()}')", "solution": "class ChatHistory:\n    def __init__(self, max_tokens=4000):\n        self.messages = []\n        self.max_tokens = max_tokens\n    \n    def add_system(self, content):\n        self.messages.append({'role': 'system', 'content': content})\n    \n    def add_user(self, content):\n        self.messages.append({'role': 'user', 'content': content})\n    \n    def add_assistant(self, content):\n        self.messages.append({'role': 'assistant', 'content': content})\n    \n    def estimate_tokens(self):\n        return sum(len(m['content']) // 4 for m in self.messages)\n    \n    def get_messages(self):\n        while self.estimate_tokens() > self.max_tokens and len(self.messages) > 2:\n            self.messages.pop(1)\n        return self.messages\n    \n    def display(self):\n        for m in self.messages:\n            print(f\"[{m['role']}]: {m['content'][:50]}...\")\n\nchat = ChatHistory(max_tokens=100)\nchat.add_system('You are a helpful tutor.')\nchat.add_user('What is Python?')\nchat.add_assistant('Python is a high-level programming language.')\nchat.add_user('How do I learn it?')\nchat.display()\nprint(f'Tokens: ~{chat.estimate_tokens()}')", "hint": "system, user, assistant for roles. pop(1) removes oldest non-system message.", "rubric": "Messages added with correct roles. Token estimation works. Display shows history."},
        {"title": "Output Parsing", "theory": "## Parsing LLM Output\nLLMs return text, but you often need structured data.\n\n```python\nimport json\n\n# Ask the LLM to respond in JSON\nprompt = 'Return a JSON object with name and age'\nresponse = '{\"name\": \"Alice\", \"age\": 25}'\n\n# Parse it\ndata = json.loads(response)\nprint(data['name'])  # Alice\n```\n\nAlways validate and handle parsing errors!", "instructions": "## Task: Response Parser\n1. Create a function that extracts JSON from LLM responses\n2. Handle cases where JSON is wrapped in markdown code blocks\n3. Validate the parsed data", "starterCode": "import json\nimport re\n\ndef parse_json_response(response):\n    # Try to extract JSON from code blocks\n    match = re.search(r'```(?:json)?\\s*(.+?)```', response, re.DOTALL)\n    if match:\n        json_str = match.group(1).strip()\n    else:\n        json_str = response.strip()\n    \n    try:\n        return json.___(___)  \n    except json.JSONDecodeError as e:\n        return {'error': str(e)}\n\n# Test with different response formats\nresponses = [\n    '{\"name\": \"Alice\", \"score\": 95}',\n    '```json\\n{\"name\": \"Bob\", \"score\": 82}\\n```',\n    'Here is the data: not valid json'\n]\n\nfor r in responses:\n    result = parse_json_response(r)\n    print(result)", "solution": "import json\nimport re\n\ndef parse_json_response(response):\n    # Try to extract JSON from code blocks\n    match = re.search(r'```(?:json)?\\s*(.+?)```', response, re.DOTALL)\n    if match:\n        json_str = match.group(1).strip()\n    else:\n        json_str = response.strip()\n    \n    try:\n        return json.loads(json_str)  \n    except json.JSONDecodeError as e:\n        return {'error': str(e)}\n\n# Test with different response formats\nresponses = [\n    '{\"name\": \"Alice\", \"score\": 95}',\n    '```json\\n{\"name\": \"Bob\", \"score\": 82}\\n```',\n    'Here is the data: not valid json'\n]\n\nfor r in responses:\n    result = parse_json_response(r)\n    print(result)", "hint": "json.loads() parses string to dict. Regex extracts JSON from code blocks.", "rubric": "Plain JSON parsed. Code block JSON extracted. Invalid JSON returns error."}
    ],
}

def apply_lessons(tracks_dir):
    from rebuild_curriculum import track_courses
    total = 0
    for track_name, levels in track_courses.items():
        safe_name = track_name.lower().replace(" ", "_").replace("&", "").replace("__", "_") + ".json"
        filepath = os.path.join(tracks_dir, safe_name)
        if not os.path.exists(filepath): continue
        with open(filepath, 'r', encoding='utf-8') as f:
            track_data = json.load(f)
        updated = False
        for course_name in track_data:
            if course_name in LESSONS_BATCH3:
                track_data[course_name]['lessons'] = LESSONS_BATCH3[course_name]
                updated = True
                total += len(LESSONS_BATCH3[course_name])
        if updated:
            with open(filepath, 'w', encoding='utf-8') as f:
                json.dump(track_data, f, indent=2, ensure_ascii=False)
    print(f'Batch 3: Applied {total} lessons')
    os.system('python build_courses.py')

if __name__ == '__main__':
    apply_lessons(os.path.join('curriculum', 'tracks'))
