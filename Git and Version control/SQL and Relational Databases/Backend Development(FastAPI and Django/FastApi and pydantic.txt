FastAPI is a modern, ultra-fast web framework for building APIs with Python 3.8+ based on standard Python type hints. Under the hood, FastAPI relies heavily on Pydantic for data validation. When a client sends a JSON payload to a FastAPI endpoint, Pydantic automatically validates the data types against a defined schema class. If the data is incorrect, it automatically rejects the request with a detailed error message before your core logic even runs.